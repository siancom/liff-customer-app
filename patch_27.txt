"[{\"AllowMultiple\":false,\"EndLine\":4428,\"ReplacementContent\":\"                {shopTab === 'products' && availableCategories.length > 1 && (\\n                    <div className=\\\"flex gap-2 overflow-x-auto hide-scrollbar pt-3 pb-1 snap-x\\\">\\n                        {availableCategories.map((cat, idx) => (\\n                            <button\\n                                key={idx}\\n                                onClick={() => { setSelectedCategory(String(cat)); setShopDisplayLimit(6); setIsPaginating(false); }}\\n                                className={`snap-center shrink-0 px-3.5 py-1.5 text-[11px] font-bold rounded-full border transition-all whitespace-nowrap shadow-sm ${\\n                                    selectedCategory === String(cat)\\n                                        ? 'bg-blue-50 border-blue-500 text-blue-700'\\n                                        : 'bg-white border-gray-200 text-gray-500 hover:border-gray-300 hover:text-gray-800'\\n                                }`}\\n                            >\\n                                {String(cat)}\\n                            </button>\\n                        ))}\\n                    </div>\\n                )}\",\"StartLine\":4412,\"TargetContent\":\"                {availableCategories.length > 1 && (\\n                    <div className=\\\"flex gap-2 overflow-x-auto hide-scrollbar pt-3 pb-1 snap-x\\\">\\n                        {availableCategories.map((cat, idx) => (\\n                            <button\\n                                key={idx}\\n                                onClick={() => { setSelectedCategory(String(cat)); setShopDisplayLimit(6); setIsPaginating(false); }}\\n                                className={`snap-center shrink-0 px-3.5 py-1.5 text-[11px] font-bold rounded-full border transition-all whitespace-nowrap shadow-sm ${\\n                                    selectedCategory === String(cat)\\n                                        ? (shopTab === 'products' ? 'bg-blue-50 border-blue-500 text-blue-700' : 'bg-teal-50 border-teal-5\n<truncated 378 bytes>"