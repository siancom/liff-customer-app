"[{\"AllowMultiple\":false,\"EndLine\":4961,\"ReplacementContent\":\"                  </button>\\n               </div>\\n\\n               {/* 🏖️ ปฏิทินวันหยุดร้านถัดไป — ให้ลูกค้าวางแผนล่วงหน้า */}\\n               {(() => {\\n                   const todayStr = getLocalDateString(new Date());\\n                   const upcoming = storeHolidays\\n                       .map(h => ({ date: h.date || h.id, reason: h.reason || '', branches: Array.isArray(h.branches) ? h.branches : ['all'] }))\\n                       .filter(h => h.date >= todayStr)\\n                       .sort((a, b) => a.date.localeCompare(b.date))\\n                       .slice(0, 4);\\n                   if (upcoming.length === 0) return null;\\n                   return (\\n                       <div className=\\\"w-full animate-in fade-in duration-500\\\">\\n                           <div className=\\\"bg-amber-50/80 border border-amber-100 rounded-[20px] p-3.5\\\">\\n                               <p className=\\\"text-[10px] font-black text-amber-700 uppercase tracking-widest mb-2 flex items-center\\\"><CalendarDays size={12} className=\\\"mr-1.5\\\"/> วันหยุดร้านที่ต้องรู้</p>\\n                               <div className=\\\"flex gap-2 overflow-x-auto hide-scrollbar pb-1\\\">\\n                                   {upcoming.map(h => {\\n                                       const isToday = h.date === todayStr;\\n                                       const isAll = h.branches.includes('all');\\n                                       return (\\n                                           <div key={h.date + h.branches.join()} className={`shrink-0 bg-white rounded-2xl px-3.5 py-2.5 border shadow-sm min-w-[110px] ${isToday ? 'border-rose-200 ring-1 ring-rose-100' : 'border-amber-100'}`}>\\n                                               <p className={`text-[11px] font-black ${isToday ? 'text-rose-500' : 'text-amber-700'}`}>\\n                          \n<truncated 1195 bytes>"