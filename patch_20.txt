"[{\"AllowMultiple\":false,\"EndLine\":520,\"ReplacementContent\":\"  const isFeatureOn = (k) => {\\n    if (featureFlags[k] !== undefined) return featureFlags[k];\\n    if (k === 'sourceOldCourses' || k === 'sourceFirestore') return false;\\n    return true; // ค่าเริ่มต้น = เปิดทุกฟีเจอร์\\n  };\",\"StartLine\":520,\"TargetContent\":\"  const isFeatureOn = (k) => featureFlags[k] !== false; // ค่าเริ่มต้น = เปิดทุกฟีเจอร์\"}]"