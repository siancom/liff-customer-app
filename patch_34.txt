"[{\"AllowMultiple\":false,\"EndLine\":4505,\"ReplacementContent\":\"                    <button onClick={() => { setShopTab('products'); setShopSearchQuery(''); setShopDisplayLimit(24); setSelectedCategory('ทั้งหมด'); }} className={`flex-1 flex justify-center items-center gap-1.5 text-[12px] font-bold py-2.5 rounded-lg transition-all ${shopTab === 'products' ? 'bg-white shadow-sm text-blue-600' : 'text-gray-500 hover:text-gray-700'}`}>\\n                        <ShoppingBag size={16} /> สกินแคร์/สินค้า\\n                    </button>\\n                    <button onClick={() => { setShopTab('courses'); setShopSearchQuery(''); setShopDisplayLimit(999); setSelectedCategory('ทั้งหมด'); }} className={`flex-1 flex justify-center items-center gap-1.5 text-[12px] font-bold py-2.5 rounded-lg transition-all ${shopTab === 'courses' ? 'bg-white shadow-sm text-teal-600' : 'text-gray-500 hover:text-gray-700'}`}>\\n                        <HeartPulse size={16} /> คอร์ส/บริการ\\n                    </button>\",\"StartLine\":4500,\"TargetContent\":\"                    <button onClick={() => { setShopTab('products'); setShopSearchQuery(''); setShopDisplayLimit(6); setSelectedCategory('ทั้งหมด'); }} className={`flex-1 flex justify-center items-center gap-1.5 text-[12px] font-bold py-2.5 rounded-lg transition-all ${shopTab === 'products' ? 'bg-white shadow-sm text-blue-600' : 'text-gray-500 hover:text-gray-700'}`}>\\n                        <ShoppingBag size={16} /> สกินแคร์/สินค้า\\n                    </button>\\n                    <button onClick={() => { setShopTab('courses'); setShopSearchQuery(''); setShopDisplayLimit(6); setSelectedCategory('ทั้งหมด'); }} className={`flex-1 flex justify-center items-center gap-1.5 text-[12px] font-bold py-2.5 rounded-lg transition-all ${shopTab === 'courses' ? 'bg-white shadow-sm text-teal-600' : 'text-gray-500 hover:text-gray-700'}`}>\\n                        <HeartPulse size={\n<truncated 1501 bytes>"