1. Modify `App.jsx` in `liff-customer-app` to process `dbMasterCourses` into individual items instead of grouped brochures.
2. Map `itemCategory` to `c["ประเภท"]` or `c["col_2"]` so the tabs match POS exactly.
3. Map `price` to `c["ราคา"]` or `c["col_6"]`.
4. Restore `BROCHURE_COURSES`.
