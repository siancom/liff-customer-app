"[{\"AllowMultiple\":false,\"EndLine\":8,\"ReplacementContent\":\"import { collection, onSnapshot, addDoc, updateDoc, doc, arrayUnion } from 'firebase/firestore';\",\"StartLine\":7,\"TargetContent\":\"import { collection, onSnapshot } from 'firebase/firestore';\"},{\"AllowMultiple\":false,\"EndLine\":64,\"ReplacementContent\":\"  const activePhoneRef = useRef(null);\\n\\n  const showToast = (msg, type = 'success') => {\\n    setToast({ msg, type });\\n    setTimeout(() => setToast(null), 3200);\\n  };\\n\\n  const handleTransferSuccess = (receipt) => setReceiptData(receipt);\\n  const handleTopUpSuccess = (receipt) => setReceiptData(receipt);\\n\\n  // 🌟 BOOKING HANDLERS 🌟\\n  const [isSubmittingBooking, setIsSubmittingBooking] = useState(false);\\n  const [bookingError, setBookingError] = useState('');\\n  const [showCancelConfirm, setShowCancelConfirm] = useState(false);\\n\\n  const openBookingModal = (course = null) => {\\n    setBookingCourse(course);\\n    setBookingStep(1);\\n    setIsBookingModalOpen(true);\\n    setGeneratedTicket(null);\\n    setBookingError('');\\n    setBookingForm({ date: '', time: '', serviceName: course ? getFuzzyKey(course, \\\"ชื่อคอส\\\") : '', branch: selectedBranch });\\n  };\\n\\n  const closeBookingModal = () => {\\n    setIsBookingModalOpen(false);\\n    setTimeout(() => setBookingStep(1), 300);\\n  };\\n\\n  const handleBookingSubmit = async (e) => {\\n    e.preventDefault();\\n    if (!bookingForm.date || !bookingForm.time || (!bookingForm.serviceName && !bookingCourse)) {\\n      setBookingError('กรุณากรอกข้อมูลให้ครบถ้วน');\\n      return;\\n    }\\n    setIsSubmittingBooking(true);\\n    try {\\n      const ticketNo = `BK${Date.now().toString().slice(-6)}`;\\n      const bookingData = {\\n        date: bookingForm.date,\\n        time: bookingForm.time,\\n        serviceName: bookingCourse ? getFuzzyKey(bookingCourse, \\\"ชื่อคอส\\\") : bookingForm.serviceName,\\n        branch: selectedBranch,\\n        customerName: getFuzzyKey(customerData, \\\"ชื่อ\n<truncated 2151 bytes>"