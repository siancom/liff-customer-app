"[{\"AllowMultiple\":false,\"EndLine\":980,\"ReplacementContent\":\"       const code = String(getFuzzyKey(c, [\\\"รหัสคอส\\\", \\\"col_1\\\"]) || '');\\n       if (code && !code.startsWith('F-')) return null; // ซ่อนคอร์สเก่า (old_courses)\\n       \\n       if (!rawName || status === 'ร่าง' || status === 'draft' || status === 'ปิดใช้งาน' || status === 'ปิด' || status === 'ไม่แสดง' || status === 'ซ่อน') return null;\",\"StartLine\":980,\"TargetContent\":\"       if (!rawName || status === 'ร่าง' || status === 'draft' || status === 'ปิดใช้งาน' || status === 'ปิด' || status === 'ไม่แสดง' || status === 'ซ่อน') return null;\"}]"