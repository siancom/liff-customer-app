"[{\"AllowMultiple\":false,\"EndLine\":4712,\"ReplacementContent\":\"                       {COURSE_TABS.map(tab => {\\n                           const tabCount = currentTabItems.filter(i => {\\n                               if (i.isBrochure) return false;\\n                               if (tab.id === 'all') return getCourseCategory(i) !== 'old_courses';\\n                               return getCourseCategory(i) === tab.id;\\n                           }).length;\\n                           \\n                           return (\\n                               <button\\n                                   key={tab.id}\\n                                   onClick={() => setActiveCourseTab(tab.id)}\\n                                   className={`px-4 py-2 rounded-full text-[13px] font-bold transition-all shadow-sm flex items-center gap-1.5 ${activeCourseTab === tab.id ? 'bg-[#ff3b6b] text-white border-transparent shadow-[#ff3b6b]/30' : 'bg-gray-50 text-gray-600 border border-gray-200 hover:bg-gray-100'}`}\\n                               >\\n                                   {tab.label}\\n                                   {tabCount > 0 && (\\n                                       <span className={`flex items-center justify-center min-w-[20px] h-[20px] rounded-full text-[10px] px-1 shadow-sm ${activeCourseTab === tab.id ? 'bg-white text-[#ff3b6b]' : 'bg-gray-200 text-gray-600'}`}>\\n                                           {tabCount}\\n                                       </span>\\n                                   )}\\n                               </button>\\n                           );\\n                       })}\",\"StartLine\":4704,\"TargetContent\":\"                       {COURSE_TABS.map(tab => (\\n                           <button\\n                               key={tab.id}\\n                               onClick={() => setActiveCourseTab(tab.id)}\\n                               className={`px-4 py-2 rounded-full text-[13px] font-bold transition-all shadow-sm ${activeCourseTab === tab.id ? 'bg-[#ff3b6b] text-white border\n<truncated 249 bytes>"