"[{\"AllowMultiple\":false,\"EndLine\":5666,\"ReplacementContent\":\"                                   const isHoliday = storeHolidays.some(h => {\\n                                       if (h.id !== shortDate.fullValue && h.date !== shortDate.fullValue) return false;\\n                                       if (!h.branches || h.branches.includes('all')) return true;\\n                                       const cleanBookingBranch = (bookingForm.branch || '').replace(/^สาขา/, '').trim();\\n                                       return h.branches.some(b => (b || '').replace(/^สาขา/, '').trim() === cleanBookingBranch);\\n                                   });\",\"StartLine\":5666,\"TargetContent\":\"                                   const isHoliday = storeHolidays.some(h => (h.id === shortDate.fullValue || h.date === shortDate.fullValue) && (!h.branches || h.branches.includes('all') || h.branches.includes(bookingForm.branch)));\"},{\"AllowMultiple\":false,\"EndLine\":6083,\"ReplacementContent\":\"                                                            const isHoliday = storeHolidays.some(h => {\\n                                                                if (h.id !== shortDate.fullValue && h.date !== shortDate.fullValue) return false;\\n                                                                if (!h.branches || h.branches.includes('all')) return true;\\n                                                                const cleanCartBranch = (cartBookingForm.branch || '').replace(/^สาขา/, '').trim();\\n                                                                return h.branches.some(b => (b || '').replace(/^สาขา/, '').trim() === cleanCartBranch);\\n                                                            });\",\"StartLine\":6083,\"TargetContent\":\"                                                            const isHoliday = storeHolidays.some(h => (h.id === shortDate.fullValue || h.date === shortDate.fullValue) && (!h.branches || h.branches.includes('all') || h.branches.includes(cartBookingForm\n<truncated 14 bytes>"