"[{\"AllowMultiple\":false,\"EndLine\":933,\"ReplacementContent\":\"             const status = String(getFuzzyKey(p, [\\\"สถานะ\\\", \\\"status\\\", \\\"การใช้งาน\\\", \\\"state\\\"]) || '').trim();\\n             if (status === 'ร่าง' || status === 'draft' || status === 'ปิดใช้งาน' || status === 'ปิด' || status === 'ซ่อน' || status === 'ไม่แสดง') return { id: p.id, name: 'ไม่มีชื่อ' }; // Will be filtered out\\n\\n             return { id: p.id, name, desc, price, originalPrice, type, image, images: imageList, wooCategories: '', wooType: 'simple', groupedIds: [], itemCategory }; \\n         }).filter(p => p.name !== 'ไม่มีชื่อ');\",\"StartLine\":932,\"TargetContent\":\"             return { id: p.id, name, desc, price, originalPrice, type, image, images: imageList, wooCategories: '', wooType: 'simple', groupedIds: [], itemCategory }; \\n         }).filter(p => p.name !== 'ไม่มีชื่อ');\"},{\"AllowMultiple\":false,\"EndLine\":945,\"ReplacementContent\":\"      dbCourses.forEach(c => {\\n         const name = String(getFuzzyKey(c, [\\\"ชื่อคอส\\\", \\\"ชื่อคอร์ส\\\", \\\"col_8\\\"]) || '').trim();\\n         const status = String(getFuzzyKey(c, [\\\"สถานะ\\\", \\\"status\\\", \\\"การใช้งาน\\\", \\\"state\\\"]) || '').trim();\\n         const price = parseNumber(getFuzzyKey(c, [\\\"ราคา\\\", \\\"ราคาเต็ม\\\", \\\"col_6\\\"]));\\n         const alreadyExists = items.some(i => i.name === name);\\n         if (status === 'ร่าง' || status === 'draft' || status === 'ปิดใช้งาน' || status === 'ปิด' || status === 'ซ่อน' || status === 'ไม่แสดง') return;\\n         \\n         if (name && name !== '-' && price > 0 && !alreadyExists) {\\n             if (!extractedCatalog.has(name) || extractedCatalog.get(name).price < price) {\",\"StartLine\":939,\"TargetContent\":\"      dbCourses.forEach(c => {\\n         const name = String(getFuzzyKey(c, [\\\"ชื\n<truncated 423 bytes>"