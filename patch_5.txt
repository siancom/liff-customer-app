"[{\"AllowMultiple\":false,\"EndLine\":5666,\"ReplacementContent\":\"                                   const isHoliday = storeHolidays.some(h => (h.id === shortDate.fullValue || h.date === shortDate.fullValue) && (!h.branches || h.branches.includes('all') || h.branches.includes(bookingForm.branch)));\",\"StartLine\":5666,\"TargetContent\":\"                                   const isHoliday = storeHolidays.some(h => h.id === shortDate.fullValue || h.date === shortDate.fullValue);\"},{\"AllowMultiple\":false,\"EndLine\":6083,\"ReplacementContent\":\"                                                            const isHoliday = storeHolidays.some(h => (h.id === shortDate.fullValue || h.date === shortDate.fullValue) && (!h.branches || h.branches.includes('all') || h.branches.includes(cartBookingForm.branch)));\",\"StartLine\":6083,\"TargetContent\":\"                                                            const isHoliday = storeHolidays.some(h => h.id === shortDate.fullValue || h.date === shortDate.fullValue);\"}]"